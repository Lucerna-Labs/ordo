# Public Copy Notes

This is a sanitized source copy prepared for GitHub upload.

Excluded from this copy:

- Git worktree metadata
- build outputs and debug artifacts
- node_modules and bundled frontend dist output
- local runtime data and databases
- user-files, skills, local MCP data, and personal runtime configuration
- local logs and environment files
- private/internal working notes

After cloning, install dependencies and rebuild locally instead of relying on copied artifacts.
